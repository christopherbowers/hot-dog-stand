import React from "react";

const OpenShop = (props) => {
  console.log(props.mains)
  // const burgerCount = parseInt(props.mains[0].quantity)
  // const burgersSold = Math.floor(Math.random * burgerCount);
  // const burgersLeft = burgerCount - burgersSold;

  // console.log(burgersLeft);

  return (
    <div>
      <h2>Open Shop</h2>

    </div>
  )
}

export default OpenShop;